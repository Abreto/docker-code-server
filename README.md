# docker-code-server

This is a personalized image comparing to office's one, updated to date.

RUN
```
$ docker run -dt \
    -h code-abreto-net \
    abreto/code-server
```

## Remember
Change `JDKVER` when needed.
